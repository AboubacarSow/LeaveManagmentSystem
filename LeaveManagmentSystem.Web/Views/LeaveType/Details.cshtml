@model LeaveTypeReadOnlyVM

@{
    ViewData["Title"] = $" Leave Type {Model.Name}'s Details";
}

<h3 class="text-center">@Model.Name  Details</h3>

<div>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Days)
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Days)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.Id" class="btn btn-warning"> <i class="fa-solid fa-pen"></i>Edit</a> |
    <a asp-action="Index" class="btn btn-primary"> <i class="fa-solid fa-arrow-left"></i> Back to List</a>
</div>
